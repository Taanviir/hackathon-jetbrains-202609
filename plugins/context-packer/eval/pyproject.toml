[project]
name = "context-packer-eval"
version = "0.1.0"
description = "Agent A/B: the same agent finds the files for real Koog tasks, with and without pack_context."
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "mcp>=2.2.0",
    "python-dotenv>=1.0",
    "typesafe-sdk>=0.7.1",
]

[tool.uv]
package = false
