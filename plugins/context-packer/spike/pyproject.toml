[project]
name = "context-packer-spike"
version = "0.1.0"
description = "Measure how well and how fast Jev picks the files a real Koog commit changed."
requires-python = ">=3.12"
dependencies = [
    "typesafe-sdk>=0.7.1",
    "python-dotenv>=1.0",
    "httpx>=0.27",
]

[tool.uv]
package = false
